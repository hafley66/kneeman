# --- build output ---
rust-sim/target/
.godot/
/build/
web/dist/
web/target/

# --- OS / editor ---
.DS_Store

# --- PRIVATE: never published to the public repo ---
# Working notes + cross-session memory (kept local).
AGENTS.md
# Session transcripts.
chat_log/
# Reference / prior-art repos pulled as submodules. On disk, never committed.
.ext/
.gitmodules
# CC0 test art lives on disk + is bundled into web builds locally; not in the source repo.
assets/
# pose-capture ghost refs: sliced from downloaded workshop art (make_refs.py regenerates)
tools/pose-capture/ref/

# old git history, backed up before the clean re-init
.git-old/

# local agent worktrees/scratch
.claude/
signaling/target/
rust-sim/vendor/*/target/
**/target/

# --- tooling scratch (asset-fetcher venv + downloaded-pack cache) ---
tools/.venv/
tools/.cache/
**/.venv/
__pycache__/

# pulled pose captures (tools/pose-capture -> just poses-pull)
captures/
